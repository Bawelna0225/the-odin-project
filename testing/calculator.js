const add = (a, b) => {
    return a + b
}
const subtract = (a, b) => {
    return a - b
}
const multiply = (a, b) => {
    return a * b
}
const divide = (a, b) => {
    return a / b
}

export const calculator = {
    add: add,
    subtract: subtract,
    multiply: multiply,
    divide: divide
}
